# solvela-ts

> **This SDK has moved.** Source now lives in the Solvela monorepo:
> https://github.com/solvela-ai/solvela/tree/main/sdks/typescript

**Install (unchanged):**

```bash
npm install @solvela/sdk
```

The npm package name `@solvela/sdk` is preserved — existing installs and
`import` paths continue to work without changes.

Issues, PRs, and discussion: https://github.com/solvela-ai/solvela/issues

The monorepo's `sdks/typescript/` is byte-equivalent to this repo's `main` at
archive time, with `package.json` `repository`/`homepage`/`bugs` rewritten to
point at the monorepo.
